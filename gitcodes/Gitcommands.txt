Git >> Version Control

    v1
    v2
    v3
new v4


>> It is distributed (on Cloud)
>> Collaboration
>> Tracking Changes
>> Recovery

https://git-scm.com/downloads

GitHub
BitBucket
GitLabs
GitPod

>> For 1st time need to configure github in our system
git config --global user.name "prashant4833o"
git config --global user.email "prashant4833o@gmail.com"

git config --global color.ui auto


Mandatory Commands:
>> git init
>> git remote add origin https://github.com/prashant4833o/GitDemo.git



git pull origin main
git status

git add .
git status
git commit -m "Commit Message"
git push --set-upstream origin master

create a new branch 
git checkout -b "branchName"

change branch
git checkout branchname

to merge other branches(dummy1 to main)
git pull origin main
git checkout main
git merge dummy1
git push -u origin main

remove old repo origins locally
git remote remove origin

Force push your local branch to overwrite the remote branch
git push -u --force origin master:main

First, pull the remote branch, merge conflicts, then push.
git pull origin main
git push -u origin master:main
